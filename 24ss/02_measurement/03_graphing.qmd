# Darstellung von Daten

<!----------------->
<!--- New slide --->
<!----------------->
<!-- ::: {.content-hidden when-format="pdf"}
## Der Forschungsprozess {.hcenter-slide}


```yaml { .animate src="images/scientific_process.svg"}
setup:
    - element: "#data"
      modifier: function() { this.node.style.fill = 'green'; }
    - element: "#databg"
      modifier: function() { this.node.style.fill = '#d8ffe2';}
```
::: -->




<!----------------->
<!--- New slide --->
<!----------------->
## Darstellung kategorialer Daten: die Häufigkeitstabelle

- **Reminder:** Kategoriale Variable = qualitative Ausprägungen; keine natürliche Reihenfolge

:::{.fragment}
:::: {.columns}
::: {.column width="75%"}

<!---  Table --->
| Haarfarbe | Absolute Häufigkeit (n) | Relative Häufigkeit (%) |
|-|-|-|
| Blau (gefärbt) | 440 | 55 % |
| Grün (gefärbt) | 152 | 19 % |
| Braun (natürlich) | 104 | 13 % |
| Blond (natürlich) | 64 | 8 % |
| Schwarz (natürlich) | 40 | 5 % |

: Häufigkeitsverteilung von Haarfarben unter Paradoxikern (N=800) {tbl-colwidths="[33.3, 33.3, 33.3]"}
:::


<!-- Begin second column -->
::: {.column width="25%"}

![Bildnachweis^[https://www.holleewoodhair.com/rainbow-hair-color/]](images/rainbow_hair.png)

:::
::::
:::


- [**Absolute Häufigkeiten:**]{color="navy"} geben an, wie oft die jeweiligen Ausprägungen einer Variable vorkommen (Anzahl)
- [**Relative Häufigkeiten:**]{color="navy"} geben an, wie häufig die jeweiligen Ausprägungen in Bezug zu allen Fällen vorkommen (Prozent oder Anteil)




<!----------------->
<!--- New slide --->
<!----------------->
## Darstellung kategorialer Daten: die Häufigkeitstabelle

![Darstellung von Häufigkeitstabellen in JASP](images/frequency_table_jasp.png)


<!----------------->
<!--- New slide --->
<!----------------->
## Darstellung kategorialer Daten: Kreisdiagramme

:::: {.columns}
::: {.column width="58%"}

- Vorteil: übersichtliche Darstellung der Verteilung kategorialer Variablen
- Nachteil: verschiedene Bedingungen können nicht direkt verglichen werden (zwei Kreisdiagramme notwendig)

<div class="vspace-medium"></div>

:::{.fragment}
![Kreisdiagramm in JASP](images/kreisdiagramm_jasp.png){width=520}
:::

:::


<!-- Begin second column -->
::: {.column width="42%"}

:::{.fragment}
![](images/haircolor.png)
:::

:::
::::


<!----------------->
<!--- New slide --->
<!----------------->
## Darstellung kategorialer Daten: Häufigkeitsdiagramm

- Das Häufigkeitsdiagramm erlaubt einen besseren Vergleich von zwei Gruppen oder Bedingungen

<!-- ```{python}
#| label: fig-bargraph-example
#| fig-cap: "Verteilung der Haarfarbe bei Paradoxikern und Kontrollen in Form eines Häufigkeitsdiagramms. Die Form der Darstellung wird auch als Balken- oder Säulendiagramm bezeichnet."

import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

np.random.seed(3)
root = '/home/matteo/OneDrive/lehre/Statistik/stats1_lecture/book/'

fontsize = 14
order = ['Schwarz', 'Blond', 'Braun', 'Gruen', 'Blau']

df = pd.read_csv('data/haircolor_raw.csv')

plt.figure(figsize=(12, 4.5))

plt.subplot(121)
count0 = df[df.group == 0].groupby('haircolor').id.count().reindex(order)
count1 = df[df.group == 1].groupby('haircolor').id.count().reindex(order)
plt.bar(np.arange(0, 12.5, 2.5), count0.values, label='Paradoxia')
plt.bar(np.arange(1, 13.5, 2.5), count1.values, label='Kontroll')
plt.xticks(np.arange(0.5, 12.5, 2.5), count0.index, fontsize=fontsize)
plt.yticks(fontsize=fontsize)
plt.ylabel('Anzahl', fontsize=fontsize)
plt.title('Vertikale Darstellung', fontsize=fontsize, fontweight='bold')
plt.legend(fontsize=fontsize)

plt.subplot(122)
count0 = df[df.group == 0].groupby('haircolor').id.count().reindex(order)
count1 = df[df.group == 1].groupby('haircolor').id.count().reindex(order)
plt.barh(np.arange(0, 12.5, 2.5), count0.values, label='Paradoxia')
plt.barh(np.arange(1, 13.5, 2.5), count1.values, label='Kontroll')
plt.xticks(fontsize=fontsize)
plt.yticks(np.arange(0.5, 12.5, 2.5), ['Lange Variante von '+ label if label == 'Schwarz' else label for label in count0.index], fontsize=fontsize)
plt.xlabel('Anzahl', fontsize=fontsize)
plt.title('Horizontale Darstellung', fontsize=fontsize, fontweight='bold')
plt.legend(fontsize=fontsize)

plt.tight_layout()
plt.savefig(os.path.join(root, 'images', 'histogram_categorical.png'), bbox_inches="tight")
``` -->

<div class="vspace-medium"></div>

:::{.fragment}
![Vertikale und horizontale Darstellung von Häufigkeiten. Die Horizontale Darstellung hat den Vorteil, dass längere Beschriftungen der Balken möglich sind. Die Darstellung mit vertikalen Balken wird zuweilen **Säulendiagramm** genannt und die Darstellung mit horizontalen Balken **Balkendiagramm**.](images/histogram_categorical.png)
:::

<!----------------->
<!--- New slide --->
<!----------------->
## Darstellung metrischer Daten: das Histogramm

- Erinnerung: metrische Variablen sind Zahlenwerte (diskret oder kontinuierlich)
- Beispiele: Körpergröße (z.B. in *cm*), durchschnittliches Zuspätkommen einer Kohorte in Statistik 1 (z.B. in *sec*), emotionaler Intelligenzquotient (einheitslos)

::: {.fragment}
Eine gängige Darstellungsform metrischer Daten ist das [**Histogramm**]{color="navy"} mit **absoluter** oder **relativer Häufigkeit**:
:::

```yaml { .animate src="images/histogram_example.svg"}
setup:
  - element: "#image1"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "3"
  - element: "#title"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "3"        
  - element: "#binsize_text"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "4"
  - element: "#binsize_bracket"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "4"
  - element: "#totalbg"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "5"
  - element: "#total"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "5"        
  - element: "#img_relative"
    modifier: attr
    parameters:
      - class: fragment
        data-fragment-index: "6"                
```

